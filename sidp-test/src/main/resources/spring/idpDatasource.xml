<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xmlns:aop="http://www.springframework.org/schema/aop"
  xsi:schemaLocation="http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd http://www.springframework.org/schema/aop http://www.springframework.org/schema/aop/spring-aop.xsd"
  default-lazy-init="true">

    <!--<bean id="keyProvider" class="com.tallate.sidp.keyprovider.DefaultKeyProvider"/>-->
    <bean id="keyProvider" class="com.tallate.test.keyprovider.SpanIdKeyProvider"/>

    <bean id="keyStore" class="com.tallate.sidp.store.SQLKeyStore">
        <property name="dataSource" ref="idpDataSource"/>
    </bean>

    <bean id="idpChecker" class="com.tallate.sidp.idpchecker.blocking.DefaultIdpChecker">
        <property name="keyProvider" ref="keyProvider"/>
        <property name="keyStore" ref="keyStore"/>
    </bean>

    <bean id="idpInterceptor" class="com.tallate.sidp.IdpInterceptor">
        <property name="idpChecker" ref="idpChecker"/>
    </bean>

    <bean id="idpAspect" class="com.tallate.sidp.spring.IdpAspect">
        <property name="idpInterceptor" ref="idpInterceptor"/>
    </bean>

    <aop:config>
        <aop:aspect id="idp" ref="idpAspect" order="2">
            <aop:pointcut id="addEnableIdp" expression="@annotation(com.tallate.sidp.EnableIdp)"/>
            <aop:around method="intercept" pointcut-ref="addEnableIdp"/>
        </aop:aspect>
    </aop:config>

    <!-- 这个Aspect获取Http请求信息，注意order值比上边那个小、优先执行 -->
    <bean id="httpAspect" class="com.tallate.test.http.HttpAspect"/>
    <aop:config>
        <aop:aspect id="http" ref="httpAspect" order="1">
            <aop:pointcut id="getSpanId" expression="@annotation(com.tallate.sidp.EnableIdp)"/>
            <aop:around method="intercept" pointcut-ref="getSpanId"/>
        </aop:aspect>
    </aop:config>

    <!-- 数据源配置 -->
    <bean id="idpDataSource" class="com.alibaba.druid.pool.DruidDataSource" destroy-method="close">
        <!-- Connection Info -->
        <property name="url" value="${jdbc.url}"/>
        <property name="username" value="${jdbc.username}"/>
        <property name="password" value="${jdbc.password}"/>
        <property name="driverClassName" value="${jdbc.driver}"/>

        <property name="defaultAutoCommit" value="true"/>

        <!-- Connection Pooling Info -->
        <property name="maxActive" value="${jdbc.pool.maxActive}"/>
        <!--<property name="maxIdle" value="${jdbc.pool.maxIdle}"/>-->
        <property name="minIdle" value="${jdbc.pool.minIdle}"/>
        <property name="maxWait" value="${jdbc.pool.maxWait}"/>
        <property name="minEvictableIdleTimeMillis"
          value="${jdbc.pool.minEvictableIdleTimeMillis}"/>
        <property name="removeAbandoned" value="${jdbc.pool.removeAbandoned}"/>
        <property name="removeAbandonedTimeout" value="${jdbc.pool.removeAbandonedTimeout}"/>

        <!-- 自动重连配置 -->
        <property name="testWhileIdle" value="true"/>
        <property name="validationQuery" value="select 1"/>
        <property name="validationQueryTimeout" value="1"/>
    </bean>

</beans>